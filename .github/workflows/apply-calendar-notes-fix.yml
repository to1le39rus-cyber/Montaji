name: Legacy patch workflow (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy runtime patch workflow is intentionally disabled. Canonical app.js is the source of truth."