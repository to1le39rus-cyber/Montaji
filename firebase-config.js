// Kept empty intentionally. app-v4 runs local-first.
// Cross-device synchronization is handled exclusively by firebase-sync.js
// using firebase-shared-config.js, so there is one canonical sync layer.
export const firebaseConfig = {};
