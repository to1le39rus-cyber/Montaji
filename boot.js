/* MONТАЖИ — single application entry point.
   All application logic lives in app.js. No runtime source rewriting,
   no post-load patches, no duplicate Firebase clients. */
import './app.js?v=20260824-1';
