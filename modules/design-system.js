/* Shared UI primitive hooks. Feature code can import these helpers instead of inventing new classes. */
export const UI_CLASSES = Object.freeze({
  primary: 'primary',
  secondary: 'secondary',
  danger: 'danger',
  action: 'action-chip',
  status: 'status-pill',
});
