/* Montaji Design System — single source of truth for UI primitives. */
export const UI = Object.freeze({
  radius: Object.freeze({ sm: '10px', md: '14px', lg: '18px', xl: '24px', pill: '999px' }),
  motion: Object.freeze({ fast: '120ms', normal: '180ms', sheet: '280ms' }),
  spacing: Object.freeze({ xs: '4px', sm: '8px', md: '12px', lg: '16px', xl: '24px', xxl: '32px' }),
  control: Object.freeze({ minHeight: '48px', compactHeight: '40px' }),
});
