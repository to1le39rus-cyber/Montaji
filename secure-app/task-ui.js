/* Montaji AA — Task UI intentionally disabled.
   Task creation is handled by quick-add-runtime.js through the existing note flow,
   so there is exactly one task modal and one trusted persistence path. */
(() => {})();
