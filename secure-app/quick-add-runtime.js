/* Montaji AA — Quick Add runtime intentionally disabled.
   The launcher uses the existing app markup and handlers; presentation is handled by quick-add-premium.css.
   Keeping this file as a no-op prevents a second runtime from replacing the trusted action DOM. */
(()=>{})();
